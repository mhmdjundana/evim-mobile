import ButtonExample from "@/components/button/ActionButton";
import { ScrollView } from "react-native";

export default function Buttons() {
    return (
        <ScrollView>
            <ButtonExample />
        </ScrollView>
    );
}